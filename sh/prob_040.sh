#!/bin/sh

seq 1 1000000 \
    |
tr -d '\n' \
    |
sed -e 's/./&\n/g' \
    |
awk 'NR == 1 || NR == 10 || NR == 100 || NR == 1000 || NR == 10000 || NR == 100000 || NR == 1000000' \
    |
tr '\n' '*' \
    |
sed -e 's/\*$/\n/' \
    |
bc
